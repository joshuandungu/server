# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017/ecommerce_app

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_RESET_SECRET=your_jwt_reset_secret_key_here

# Gmail SMTP Configuration
GMAIL_USER=your-email@gmail.com
GMAIL_PASS=your-gmail-app-password

# Instructions for Gmail App Password:
# 1. Go to your Google Account settings
# 2. Enable 2-Factor Authentication
# 3. Go to Security > App passwords
# 4. Generate a new app password for "Mail"
# 5. Use the 16-character password (without spaces) as GMAIL_PASS

### MPESA / Daraja configuration (sandbox vs production)
# MPESA_ENV: "sandbox" or "production". Use sandbox for local testing.
MPESA_ENV=sandbox

# OAuth credentials (get from Daraja portal)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=

# Business Shortcode and Passkey (STK Push)
MPESA_SHORTCODE=
MPESA_PASSKEY=

# Publicly reachable callback base URL used by the server.
# Important: do NOT include a trailing slash beyond /callback
# Example: https://gas-app-backend-1gjx.onrender.com/api/mpesa/callback
MPESA_CALLBACK_URL=

# Optional extra check - the server checks this query param on callbacks
MPESA_CALLBACK_SECRET=

# (Optional) For TransactionStatus queries you may need:
MPESA_INITIATOR=
MPESA_SECURITY_CREDENTIAL=
